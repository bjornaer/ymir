#@ case quantum/no_cloning
#@ spec 08-quantum.md#no-cloning
#@ skip gate names are unresolved (open question Q14); the linearity rule it asserts is covered by quantum/no_cloning_by_move
#@ compile-error
#@ | moved

module no_cloning

func main() {
    q1 := qubit()
    q2 := q1
    h(q1)
    discard(q2)
}
