#@ case expr/call_arity_mismatch
#@ spec 04-expressions.md#calls
#@ compile-error
#@ | takes 2 arguments

module call_arity_mismatch

func add(a: int, b: int) -> int {
    return a + b
}

func main() {
    print(add(1))
}
