#@ case expr/multi_valued_call_nested
#@ spec 04-expressions.md#calls
#@ compile-error
#@ | multi-valued call

module multi_valued_call_nested

func divmod(a: int, b: int) -> (int, int) {
    return a / b, a % b
}

func main() {
    print(divmod(17, 5))
}
